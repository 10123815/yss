#ifndef _ENTITY_H_
#define _ENTITY_H_

namespace ysd_simple_server
{

	///////////////////////////////////////////////////
	// An Entity denotes a player in game lobby,
	// the logic of lobby server comes from the
	// interaction of Entitys.
	// An Entity get data from gateway server, process
	// them, then all Entity send the processed data to
	// the peer clients.
	///////////////////////////////////////////////////
	class Entity
	{
	public:
		
	};
}

#endif